import { Center, Loader as MantineLoader } from '@mantine/core';

const Loader = () => {
  return (
    <Center mt="xl">
      <MantineLoader size="lg" />
    </Center>
  );
};

export default Loader;
