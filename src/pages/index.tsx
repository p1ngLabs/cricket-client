import { NextPage } from 'next/types';
import Layout from '@layouts/Layout';

const Home: NextPage = () => {
  return (
    <Layout>
      <div>this is our landing page...</div>
    </Layout>
  );
};

export default Home;
